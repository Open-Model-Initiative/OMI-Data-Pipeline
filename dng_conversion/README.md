# Setup
```shell
python -m venv venv
source venv/bin/activate
# .\env\Scripts\activate
pip install --upgrade pip
pip install -r dng-requirements.txt
```

# Run

```shell
python convert_dng_to_jpg.py test_01.dng test_01.jpg
```
